# Enneagram Test

A self-contained, single-file Enneagram personality test (72 questions, nine types,
shareable/downloadable results). Everything lives in `index.html` — no build step,
no dependencies. Deployed as a static site on Vercel; every push to `main` redeploys.
